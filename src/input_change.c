/*
** EPITECH PROJECT, 2020
** input
** File description:
** this file change the input of the game
*/

#include "debug.h"
#include <stdlib.h>

debug_t *change_input(char *str)
{
    debug_t *info = NULL;

    info = malloc(sizeof(debug_t));

    if (my_strncmp(str, "-d") == 0)

    if (my_strncpm(str, "--key_turn") == 0)

    if (my_strncmp(str, "-q") == 0)
}